# Log Analytics workspace ID (GUID format)
# Get this from Azure Portal > Log Analytics workspace > Properties > Workspace ID
LOG_ANALYTICS_WORKSPACE_ID=your-workspace-id-here

# Optional: Multiple workspace IDs (comma-separated)
# LOG_ANALYTICS_WORKSPACE_IDS=workspace1-id,workspace2-id

# Optional: Azure authentication settings
# Leave blank to use DefaultAzureCredential (recommended)
# This will try: Environment -> Managed Identity -> Azure CLI -> etc.
# AZURE_TENANT_ID=
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
